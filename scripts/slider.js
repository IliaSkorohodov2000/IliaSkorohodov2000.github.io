function first(numb){
	if(numb === null) numb == 1;
    setInterval(second(numb), 2000);
    }

function second(numb){
	var ourradio = document.getElementById('switch"' + numb + '"').checked=true;
}